name: research-client-static

on:
  push:
    branches:
      - client-research-fixed
  pull_request:
    paths:
      - "client/research/**"
      - "client/preservation/**"
      - "scripts/build-research-client.ps1"
      - "scripts/install-research-client.ps1"
      - "scripts/extract-xapk-specimen.py"
      - "scripts/run-research-trace.py"
      - "scripts/apply-client-patches.py"
      - "tests/test_apply_client_patches.py"
      - ".github/workflows/research-client-static.yml"

jobs:
  static-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compile Python client helpers
        run: |
          python -m py_compile scripts/extract-xapk-specimen.py
          python -m py_compile scripts/run-research-trace.py
          python -m py_compile scripts/apply-client-patches.py

      - name: Test guarded client patch pipeline
        run: python -m unittest tests.test_apply_client_patches -v

      - name: Parse Frida JavaScript
        run: |
          node --check client/research/frida/cgss-trace.js
          node --check client/research/frida/cgss-local-dns.js
          node --check client/research/frida/cgss-unitytls-trace.js
          node --check client/research/frida/cgss-ca-store-trace.js

      - name: Parse PowerShell scripts
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $files = @(
            'scripts/build-research-client.ps1',
            'scripts/install-research-client.ps1'
          )
          foreach ($file in $files) {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              (Resolve-Path $file),
              [ref]$tokens,
              [ref]$errors
            ) | Out-Null
            if ($errors.Count -gt 0) {
              $errors | ForEach-Object { Write-Error $_.Message }
              throw "PowerShell parse failed: $file"
            }
          }
