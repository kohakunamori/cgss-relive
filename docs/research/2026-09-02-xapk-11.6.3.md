# 2026-09-02 — CGSS Android 11.6.3 XAPK analysis

## Result

The supplied XAPK is now the M0 working specimen.

- package: `jp.co.bandainamcoent.BNEI0242`
- app: `11.6.3` / versionCode `438`
- APK layout: base + arm64-v8a + armeabi-v7a
- runtime: Unity **2022.3.56f1**, IL2CPP metadata **v31**
- current API/CDN literals, request-header vocabulary, MessagePack and AES-oriented transport components confirmed from the final client metadata
- current binary contains resource-version literal **10133000**
- separate maintained final-resource tooling points at **10133800**, so application and final resource revisions must be modeled independently

See [`../specimens/android-11.6.3-xapk.md`](../specimens/android-11.6.3-xapk.md) for hashes and structural details.

## Immediate reverse targets

1. Resolve `Cute.NetworkTask.PreparePostData`, `CreateBody`, `AddHeaderParam`, `SetResponseData` native xrefs.
2. Resolve `Cute.CryptAES` / `Cute.Cryptographer` call graph used by `NetworkTask`.
3. Resolve `Cute.VersionCheckTask` and `Cute.Certification.VersionCheckTaskExec` call path to `load/check`.
4. Confirm current body envelope and `PARAM` / `SID` construction against final-client behavior; historical formulas are references only.
5. Freeze resource revision 10133800 and determine exactly how `required_res_ver` advances 11.6.3 from bundled/default 10133000.
